{
  "buildCommand": "cd ../.. && pnpm install && pnpm build --filter=@nextplate/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
